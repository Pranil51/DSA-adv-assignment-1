def checksum(s,l):
    count=0
    for i in range(len(l)):
        for j in range(i+1,len(l)):
             if l[i]+l[j]==s:
                 print(f"{l[i]}+{l[j]}")
                 count+=1
             else:
                 pass
    return count
checksum()
